/** @jsx jsx */
import { jsx } from "@emotion/core";
import { Heading, Box } from "rebass";


const Section = ({ copyrightNotice }) => (
  <Box>
    <Heading color="pink" variant="heading">Local component</Heading>
  </Box>
);

export default Section;
